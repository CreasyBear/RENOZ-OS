/**
 * Modal Components
 *
 * Reusable modal patterns for dialogs and confirmations.
 *
 * @example
 * ```tsx
 * import { ConfirmationModal, AlertModal, FormModal } from '~/components/shared/modals'
 * ```
 */

export { ConfirmationModal, type ConfirmationModalProps } from "./confirmation-modal"
export { AlertModal, type AlertModalProps } from "./alert-modal"
export { FormModal, type FormModalProps } from "./form-modal"
