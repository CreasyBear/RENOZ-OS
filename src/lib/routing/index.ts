/**
 * Routing Module
 *
 * Centralized exports for routing utilities and metadata.
 */

export {
  ROUTE_METADATA,
  PUBLIC_ROUTES,
  getRouteMetadata,
  getNavRoutes,
  getBreadcrumbLabel,
  isPublicRoute,
} from './routes'

export type { RouteMetadata, PublicRoute } from './routes'
