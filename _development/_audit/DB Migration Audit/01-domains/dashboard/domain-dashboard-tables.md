# Domain: Dashboard — Tables

## Drizzle
- No dashboard-specific tables or materialized views found in `drizzle/schema`.

## PRD (expected)
- `targets`
- `scheduledReports`
- `dashboardLayouts`
- Materialized views: `mv_daily_metrics`, `mv_daily_pipeline`, `mv_daily_warranty`, `mv_daily_jobs`, `mv_current_state`
