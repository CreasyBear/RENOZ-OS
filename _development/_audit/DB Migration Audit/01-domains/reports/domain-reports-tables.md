# Domain: Reports — Tables

## Drizzle
- No reports-specific tables found in `drizzle/schema`.

## PRD (expected)
- `scheduled_reports`
- `report_favorites` (or favorites stored in `user_preferences`)
- `custom_reports`
